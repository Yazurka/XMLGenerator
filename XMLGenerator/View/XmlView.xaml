<UserControl x:Class="XMLGenerator.View.XmlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:View="clr-namespace:XMLGenerator.View"
             xmlns:ViewModel="clr-namespace:XMLGenerator.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
 
    <StackPanel>
        <ContentPresenter Content="{Binding BaseFolderViewModel}"/>
        <ItemsControl ItemsSource="{Binding DisciplineViewModels}"/>
        <ContentPresenter Content="{Binding FileViewModel}"/>
        <ContentPresenter Content="{Binding IFCViewModel}"/>

        <Button HorizontalAlignment="Right" Focusable="False" Width="120" Content="+ Add dicipline" Background="Transparent" BorderBrush="Transparent"  Command="{Binding AddDiciplineCommand}"/>
        
    </StackPanel>
</UserControl>
